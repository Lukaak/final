import react from 'react'
import './ProductDetails.css'
const ProductDetails = ({data,selectedId}) => {
    return(
        <div className='hello'>
            {data.id}
        </div>
    )
}

export default ProductDetails
